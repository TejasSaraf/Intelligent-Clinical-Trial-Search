# Presentation Script

To generate the PowerPoint presentation:

```bash
pip install python-pptx
python scripts/create_presentation.py
```

Output: `ClinicalTrialSearch_Presentation.pptx` (project root)
